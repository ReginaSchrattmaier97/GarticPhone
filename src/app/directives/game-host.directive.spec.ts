import { GameHostDirective } from './game-host.directive';

describe('GameHostDirective', () => {
  it('should create an instance', () => {
    const directive = new GameHostDirective();
    expect(directive).toBeTruthy();
  });
});
